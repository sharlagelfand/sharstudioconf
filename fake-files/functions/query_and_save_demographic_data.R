query_and_save_demographic_data <- function(analysis_year) {
  code to connect to database

  long complicated SQL code or dplyr version of it %>%
    filter(year(date) == analysis_year) %>%
    a bunch of code to rename and select columns

  thirty various lines to clean up and summarise the data

  some more code to save the data
}
